= form_for @output do |f|

  %h1.new_output
    アウトプットしよう！！！

  .new_genres
    .new_genres_box
      %p.new_genres_box-genre
        ジャンル名:
        = @genre.name
        = f.hidden_field :genre_id, value: @genre.id
      %p.new_genres_box-subgenre
        サブジャンル名:
        = @subgenre.name
        = f.hidden_field :subgenre_id, value: @subgenre.id

  %p
    タイトルを入力しよう
    %br
      = f.text_field :title

  %p
    点数を入力してください
    = f.number_field :score

  %p
    説明を入力してください
    = f.text_field :explanation

  %p
    学びを入力してください
    = f.text_field :learn

  %p
    好きなことを書こう
    = f.text_field :remark

  %p
    画像を追加しよう
    = f.label :image
    = f.file_field :image

  = f.submit '登録する'